import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    width: 48,
    height: 24,
    borderRadius: 24,
    // padding: 4,
    justifyContent: 'center',
    borderWidth: 1,
  },
  dot:{
      width: 16,
      height: 16,
      borderRadius: 8,
  }
});
